# Per-company secrets generated by provision_company.sh -- never commit
# these; the template (.env.template) is the only file in companies/ meant
# to be tracked.
companies/*.env
vps
.env
.env.prod
backend/.env
frontend/.env