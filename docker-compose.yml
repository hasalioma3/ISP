version: '3.8'

services:
  backend:
    build: ./backend
    command: gunicorn isp_billing.wsgi:application --bind 0.0.0.0:8000
    volumes:
      - ./backend:/app
      - static_volume:/app/staticfiles
      - media_volume:/app/mediafiles
    env_file:
      - .env.prod
    depends_on:
      - db
      - redis
    restart: always
    ports:
      - "8000:8000"
    networks:
      - isp_network

  frontend:
    build:
      context: .
      dockerfile: ./frontend/Dockerfile
      args:
        VITE_API_URL: /api
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - static_volume:/app/staticfiles
      - media_volume:/app/mediafiles
      - ./nginx/certs:/etc/nginx/certs
    depends_on:
      - backend
    restart: always
    networks:
      - isp_network

  db:
    image: postgres:15-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data
    env_file:
      - .env.prod
    restart: always
    networks:
      - isp_network

  redis:
    image: redis:alpine
    restart: always
    networks:
      - isp_network

  celery_worker:
    build: ./backend
    command: celery -A isp_billing worker -l info
    volumes:
      - ./backend:/app
      - media_volume:/app/mediafiles
    env_file:
      - .env.prod
    depends_on:
      - backend
      - redis
    restart: always
    networks:
      - isp_network

  celery_beat:
    build: ./backend
    command: celery -A isp_billing beat -l info
    env_file:
      - .env.prod
    depends_on:
      - backend
      - redis
    restart: always
    networks:
      - isp_network

networks:
  isp_network:
    driver: bridge

volumes:
  postgres_data:
  static_volume:
  media_volume:
